# Phase 20D.2 — Official result-driven promotion

آخر تحديث: 2026-09-13.

## الحالة الحالية

**التنفيذ والتحقق المحلي وSTAGING QA: PASS. PR #44 غير مدمج. Production لم تُستخدم.**

تربط هذه الدفعة قرار الانتقال السنوي بنسخة محددة من كارت نتيجة رسمي منشور.
لا يستطيع المستخدم اختيار «مترفع/معيد/متخرج» بصورة اعتباطية؛ النظام يشتق القرار
من النتيجة المنشورة والسياسة السنوية الموثقة، ثم يعرض الوجهة المطلوبة فقط.

## عقد القرار الرسمي

| نوع السياسة | النتيجة المنشورة | القرار النظامي | هل يُقفل التسجيل؟ |
|---|---|---|---|
| غير منتهٍ | ناجح | مترفع إلى صف مختلف في سنة لاحقة | نعم |
| منتهٍ | ناجح | متخرج/نهاية مرحلة بلا تسجيل هدف | نعم |
| منتهٍ أو غير منتهٍ | راسب | معيد في الصف نفسه في سنة لاحقة | نعم |
| أي نوع | مكمل | انتظار نتيجة دور لاحق منشورة | لا |
| أي نوع | غير مكتمل/كارت جزئي | إكمال النتيجة أولًا | لا |

شروط التنفيذ:

- كارت النتيجة فعال ومنشور وكامل.
- نسخة النشر `publication_revision` مطابقة تمامًا للمعاينة.
- الطالب والسنة والصف والشعبة في الكارت تطابق تسجيل المصدر.
- السياسة السنوية `approved` أو `locked` ولها مرجع قرار غير فارغ.
- سنة الهدف لاحقة وغير فعالة أثناء الإعداد، والصف والشعبة فعالان.
- الإعادة تستخدم صف المصدر نفسه، والترفيع يستخدم صفًا مختلفًا.
- لا يوجد تسجيل لاحق أو تسجيل سابق للطالب في سنة الهدف.

## Migration 0035

`migrations/0035_official_result_promotion.sql` إضافة صرفة وتضيف:

- `student_promotion_result_decisions`: سجل append-only يثبت تسجيل المصدر، كارت
  النتيجة، رقم الكارت، نسخة النشر، نوع السياسة، حالة النجاح/الرسوب، القرار، الوجهة
  وهوية المنفذ.
- Trigger يتحقق داخل معاملة التنفيذ من المصدر والكارت والنسخة والسياسة والقرار
  والوجهة وغياب تسجيل لاحق.
- Trigger يمنع إقفال تسجيل سنوي بقرار انتقال بلا سجل نتيجة رسمية مطابق.
- Triggers تمنع تعديل أو حذف سجل القرار أو تغيير التسجيل المصدر بعد إقفاله.
- Trigger يمنع سحب كارت استُخدم فعليًا في قرار انتقال سنوي.
- `student_promotion_result_readiness` لكشف أي عدم تطابق بين القرار والتسجيل والكارت.

الهجرة لا تعيد كتابة أي قيمة أو صف تاريخي. اختبار الترقية يلتقط كل جداول التطبيق
السابقة ثم يثبت تطابقها بعد تطبيق `0035` مع FK نظيف.

## API والواجهة

- المسار `POST /api/student-enrollments/promotion/official-decisions` يعيد القرار
  المشتق لغاية 30 تسجيلًا في استعلام set-based واحد.
- المعاينة والتنفيذ الفرديان والجماعيان يتطلبان `result_card_id` ونسخة النشر
  المأخوذين من المعاينة الحديثة.
- كل قرارات الدفعة الجماعية وسجلات الانتقال تُكتب ذريًا؛ تغير نتيجة واحدة يلغي
  الدفعة كلها بدل ترك نصف الطلاب منتقلين.
- واجهة الطالب تعرض رقم الكارت والدور والنتيجة ونوع الصف والقرار المشتق، وتزيل
  أزرار اختيار الحالة يدويًا.
- الواجهة الجماعية تملأ الناجح/الراسب/المتخرج تلقائيًا، وتترك المكمل وغير المكتمل
  كصفوف متخطاة بسبب واضح. يختار المستخدم وجهة المترفع فقط؛ المعيد يحتفظ بصفه.

## سيناريوهات الأمان والتزامن

- تعديل النتيجة أو سحبها بين المعاينة والتنفيذ يعيد `409` ولا يكتب قرارًا.
- ظهور تسجيل لاحق بين المعاينة والتنفيذ الفردي يعيد `409` مع rollback كامل.
- تغير نتيجة طالب واحد في دفعة جماعية يلغي جميع قرارات الدفعة.
- إعادة إرسال الطلب المطابق ناجحة idempotently ولا تنشئ قرارًا ثانيًا.
- الإجراء اليدوي المخالف للنتيجة أو input غير صالح يفشل قبل الكتابة.
- محاولة تحديث التسجيل مباشرة من SQL بلا نتيجة رسمية يرفضها trigger.
- سجل القرار والتسجيل المقفل غير قابلين للتعديل أو الحذف التاريخي.

## نتائج التحقق المحلي

| البوابة | النتيجة |
|---|---|
| اختبارات Phase 20D.2 المستهدفة | **16/16 PASS** |
| المصفوفة الكاملة | **1491/1491 PASS، 0 fail، 0 skip** |
| Genuine Local D1 — قرارات النتيجة الرسمية | **12 checks PASS** |
| Genuine Local D1 — finance/rollback | **36 migrations، 39 checks PASS** |
| Backup/restore محلي | **36 migrations، 62 tables، exact match** |
| صف `import_jobs` الكبير | **360,009 bytes، restored exactly by parameter binding** |
| TypeScript | **PASS** |
| Frontend build | **PASS** |
| Worker build | **PASS** |
| `npm audit --audit-level=low` | **0 vulnerabilities** |
| `git diff --check` | **PASS** |

اختبار D1 الحقيقي شغّل chain الهجرات كاملًا داخل قاعدة مؤقتة، ثم أثبت القرارات
الثلاثة `promoted/repeated/graduated`، ومنع `completion`، وثبّت ثلاث نسخ نتائج،
وأعاد readiness=`healthy` و`foreign_key_check` نظيفًا. لا يستخدم runner أي config
بعيد أو بيانات مدرسة أو seed حقيقي.

تحذير Vite المعروف حول chunk `xlsx` الأكبر من 500 kB بقي تحذير أداء غير مانع
ولم ينشأ من هذه الدفعة.

## بوابة STAGING المعتمدة — نُفذت بنجاح

نُفذت الخطوات التالية قبل طلب المراجعة:

1. التأكد أن فرع التسليم مبني على `main@87419ea` وأن `0035` وحدها pending.
2. أخذ export كامل لـ`smart-school-staging-db` خارج المستودع وتسجيل الحجم وSHA-256.
3. تشغيل preflight للـmigration history وFK وكل readiness views ولقطة جميع الجداول.
4. استعادة النسخة في D1 محلية، بما فيها الصف الكبير parameter binding، وإثبات
   التطابق الكامل ثم تطبيق `0035` وحدها محليًا.
5. تطبيق `0035` على STAGING فقط إذا نجحت البوابات السابقة، بلا seed/reset.
6. QA مصادق عليه لنتائج ناجح غير منتهٍ، ناجح منتهٍ، راسب، مكمل، stale revision،
   idempotent retry وbulk rollback، ثم soft cleanup دون `DELETE`.
7. إثبات migration history وFK وreadiness وعدم تغير البيانات غير المستهدفة.

## قرار القبول الحالي

اجتازت Phase 20D.2 بوابات الاستيراد والتحقق المحلي وGitHub وCloudflare وSTAGING
والـQA المصادق عليه. يبقى قرار دمج PR #44 للمراجعة البشرية؛ لم يُنفذ الدمج.
لم يُستخدم Production أو remote seed/reset أو direct SQL `DELETE` أو force-push.

## دليل التسليم المنفذ — 13 أيلول 2026

### الاستيراد والبوابات

- base المطابق بعد fetch: `87419ea53d36344033221046cd1c76f6f1361a9b`.
- الفرع: `codex/phase-20d2-official-promotion` في worktree جديد؛ بقي `.pnpm-store/` في مساحة المستخدم دون تغيير.
- استوردت الرقعتان بالترتيب بواسطة `git am`، ببصمتي SHA-256 `E3139042C5E640F650BAD0532BD4684609D45977192357C6F738F9B5F7EE6546` و`DAA173F5405F266C7A78FFDBB66DDB3C5EA8954428961DFCD6DEA7F7397ED0E5`.
- commit الأول: `0238efdc846e62dedad91dac9923a1ce61701b95`؛ HEAD المستورد: `24c58ef2ee14d0a526e6a0b6e0c91ad17078ddb5`.
- `git show --check` لكلا commit، و`git diff --check origin/main...HEAD` ناجحة. migrations `0029`–`0034` مطابقة لنسخة الأساس، و`0035` هي الإضافة الوحيدة.
- أُعيد تشغيل كل بوابات الجدول المحلي أعلاه: `npm ci`، TypeScript، `16/16`، `1491/1491` بلا fail/skip، Local D1 `12` و`39` checks، استعادة `62` جدولًا، البناء، وaudit بلا ثغرات.
- [PR #44](https://github.com/smartschoolduhok/smart-school/pull/44) فُتح Draft بعنوان `feat(academics): bind promotion to official results`.
- [Quality Gates الأولية](https://github.com/smartschoolduhok/smart-school/actions/runs/34763301631) نجحت على HEAD المستورد نفسه خلال `5m4s`.
- [Preview الثابت المستخدم في QA](https://270e474e.smart-school-staging.pages.dev) من deployment `270e474e-60f8-4f8a-b8c3-833413921b89` وHEAD نفسه. التحقق النهائي لآخر commit توثيق موثق في checks الخاصة بالـPR.

### هوية STAGING والنسخة والاستعادة

المصدر المتتبع `wrangler.jsonc` يحدد مشروع `smart-school-staging` وقاعدة
`smart-school-staging-db` بالمعرّف `1bdb9c3d-08d6-4023-9cbc-64369d53198a`.
تطابقت الهوية البعيدة مع هذا المصدر قبل النسخ وقبل فحوص D1.

النسخة الكاملة، دون أي تعديل على export، محفوظة خارج المستودع:

`C:\Users\ibrah\Documents\SmartSchoolBackups\phase20d2-staging-20260913T144520Z\smart-school-staging-db-full-before-0035.sql`

| الدليل | النتيجة |
|---|---|
| Backup bytes | `1,237,464` |
| Backup SHA-256 | `810E0E19FE5AFCB56E21A2880AED8165E096826EA59E7B2C18558399069FB3B8` |
| preflight migration history | `35` اسمًا مميزًا ومرتبًا؛ `0034` مرة واحدة وأخيرة |
| pending قبل التطبيق | `0035_official_result_promotion.sql` وحدها |
| typed snapshots | `61` جدولًا بما فيها سجلا النظام؛ `59` جدول تطبيق |
| snapshot hash قبل التطبيق | `77d06b0ce47ee712398278709ca9888dfe711d47ea61a11a4bcf876f55b6a73b` |
| FK قبل التطبيق وبعد الاستعادة | صفر مخالفات |
| الاستعادة المحلية | `2,032` statement عادية + صف كبير عبر `16` parameter و`360,514` bound bytes |
| مساواة الاستعادة | schema وtyped rows/counts/hashes والمحتوى الكامل مطابقة للـSTAGING preflight |
| تطبيق `0035` محليًا | كل قيم الجداول التطبيقية السابقة دون تغيير، `36` هجرة، `0035` مرة واحدة وأخيرة، بلا pending |
| readiness المحلية الجديدة | `student_promotion_result_readiness = healthy` للمدارس الثلاث |

استُخدم `scripts/lib/local-d1-restore.mjs` في الاستعادة إلى Genuine Local D1
معزولة. التغيير الداخلي المتوقع الوحيد في `sqlite_sequence` كان تقدم
`d1_migrations` من `35` إلى `36`؛ عدادات جداول التطبيق لم تتغير.

### تطبيق الهجرة والفحص النهائي

طُبّقت `0035` وحدها على STAGING بين `2026-09-13 14:47:31 UTC` و`14:47:34 UTC`،
من مجلد مستقل يحتوي نسخة مطابقة من ملف الهجرة الوحيد. نفذ Wrangler `11` command.
المقارنة البعيدة مباشرة بعد التطبيق أثبتت تطابق كل قيمة في الجداول السابقة الـ`59`.

بعد QA والتنظيف أعاد الفحص النهائي:

- `36/36` migration مميزة، و`0035` مرة واحدة وأخيرة، و`No migrations to apply`.
- `PRAGMA foreign_key_check` فارغة.
- promotion readiness صحية: `3` قرارات، و`0` inconsistent للمدرسة `1`؛ المدرستان الأخريان بلا قرارات.
- publication readiness صحية: `17` كارتًا للمدرسة `1` (`1` draft، `4` published، `12` withdrawn)، وبقية المدارس بلا كروت.
- academic readiness هي `not_configured` المتوقعة للمدارس الثلاث. لم تُنشأ سياسة حقيقية أو درجات حقيقية.
- fee/payroll-school/treasury readiness صحية؛ payroll التفصيلية فارغة ومتوقعة.
- كل صف كان موجودًا قبل المهمة في الجداول التطبيقية الـ`59` بقي مطابقًا، بما في ذلك الطلاب الحقيقيون والدرجات والمالية والنتائج السابقة. الإضافات والتعديلات محصورة بسجلات QA الجديدة.

### QA المصادق عليه وواجهة الهاتف

Marker: `PH20D2-b6028291af`. أُنشئ `10` طلاب مؤقتين ومستخدمان مؤقتان وصفان وشعبتان.
لم توجد سنة مستقبلية غير فعالة مناسبة، فأنشئت السنة `id=6` باسم
`PH20D2-b6028291af FUTURE INACTIVE` تبدأ `2029-09-01`، وبقيت `is_active=0`.
استخدمت الكروت snapshots اصطناعية بمرجع `QA ONLY`؛ كارت النتيجة الجزئية المنشور
fixture اصطناعي لاختبار حماية البيانات التاريخية، ولم يُنشر عبر مسار نشر النتائج المكتملة.

| السيناريو | الدليل |
|---|---|
| نجاح غير منتهٍ | الطالب `57`، المصدر `60`، الكارت `8` عند revision `1` → `promoted` ومصدر completed وهدف واحد |
| نجاح منتهٍ | الطالب `58`، المصدر `61`، الكارت `9` عند revision `1` → `graduated` بلا هدف |
| رسوب | الطالب `59`، المصدر `62`، الكارت `10` عند revision `1` → `repeated` في الصف نفسه بالسنة اللاحقة |
| إعادة الطلب نفسه | `already_applied=true` وsnapshot القرارات والتسجيلات دون تغيير |
| completion / incomplete / draft / withdrawn | رُفضت المعاينة والتنفيذ بـ`409` دون كتابة |
| قرار يدوي مخالف / stale revision | رُفض التنفيذ بـ`409` دون كتابة |
| مدرسة أخرى | `403` دون كتابة |
| سحب كارت مستخدم | `409 official_result_already_applied` مع بقاء الكارت وسجل النشر مطابقين |
| bulk stale بعد المعاينة | سُحب الكارت `16` قبل التنفيذ؛ رُفضت الدفعة كلها وبقي المصدران والقرارات والأهداف دون أي تغيير |
| SQL transition بلا قرار | رُفض بواسطة `official_result_required` |
| ثبات الأدلة | رُفض تعديل القرار والمصدر النهائي وsnapshot الكارت وسجل النشر بواسطة triggers |
| الواجهة الفردية والجماعية | عرض الكارت والدور والنتيجة؛ لا اختيارات اعتباطية، والجماعي يتيح القرار المشتق أو التخطي فقط |
| الهاتف ولوحة المفاتيح | `390×844` وRTL، عرض المستند `390px` بلا overflow للصفحة، جدول `1050px` داخل حاوية قابلة للتمرير؛ المعاينة وفتح التأكيد والإلغاء بالكيبورد نجحت في النمطين |

نجحت `17` نقطة دليل للـAPI والإعداد والتنظيف و`9` فحوص متصفح. حُفظت خمس لقطات
للهاتف وسطح المكتب وفُحصت بصريًا. لم تظهر أخطاء JavaScript غير معالجة أو عيوب تمنع
إكمال المسار. احتاج أول تشغيل لأداة المتصفح انتظار اكتمال طلب النتيجة غير المتزامن؛
عُدل انتظار الأداة ثم نجح الفحص الكامل دون تعديل كود التطبيق.

### التنظيف وحفظ الأثر

- الطلاب `57`–`66` مؤرشفون؛ المستخدمان المؤقتان inactive؛ الصفان `26/27` وشعبتاهما مؤرشفان.
- ألغيت تسجيلات المصدر المؤقتة غير المنفذة، وسُحبت الكروت المنشورة غير المستخدمة حيث يسمح العقد.
- بقيت القرارات الثلاثة، والمصادر completed، والكروت المستخدمة منشورة عند revision `1` دون تعديل.
- احتُفظ بسجلي الهدف `active/pending` لتوافق سجل القرار وreadiness؛ الطالب والصف والشعبة مؤرشفون والسنة المستقبلية غير فعالة، فلا يصبحان تسجيلين تشغيليين نشطين. تغيير حالتهما سيكسر readiness الخاصة بالأثر النهائي.
- المسودة `id=13` محفوظة تحت طالب مؤرشف؛ تغييرها إلى cancelled مع إبقاء publication=draft يخالف readiness الحالية، وسحبها ليس انتقالًا صالحًا. بقيت معزولة دون حذف أو تزوير انتقال نشر.
- صفر مستخدم QA فعال، وصفر طالب QA فعال، ولا تفعيل للسنة المستقبلية. لم يُنفذ SQL `DELETE` مباشر أو cleanup هدّام.

### ملفات الأدلة خارج المستودع

المجلد: `C:\Users\ibrah\Documents\SmartSchoolBackups\phase20d2-staging-20260913T144520Z`.
لا تُرفع snapshots الكاملة أو بيانات الدخول إلى GitHub أو Notion.

| الملف | bytes | SHA-256 |
|---|---:|---|
| `phase20d2-readonly-preflight.json` | 3774223 | `7CD1D71F9ECBCDB5B2DBD0BEE90FE38B0BB3324F709A7915D21417C163BC6F17` |
| `phase20d2-local-rehearsal-evidence.json` | 4063 | `F2D19689FFD5059B62E3D5C7820A930D42E054B20AFB8F8BBA09171634E7FBDC` |
| `phase20d2-after-migration.json` | 3941885 | `09E4A43F94C40349A76EA4D3D3B76913BF2CCBDDF5C5D1218ABEA83A0E2C928A` |
| `phase20d2-authenticated-qa.json` | 10714 | `24413D9279206B4D3D87A2B4411DD69D65EC5F57FA7E18D9E144325853FF66F1` |
| `phase20d2-ui-qa.json` | 2298 | `2D40D4EF074B21179FFAADCB4DC63E3860181A66C50D10419A2413CF0E24CE1B` |
| `phase20d2-final-staging.json` | 3980344 | `9BE152CA058E39D79C9012725631E4F473502084455D30CE51A1801301B8C3B4` |

**حدود مثبتة:** STAGING فقط؛ لا وصول إلى Production، ولا remote seed/reset، ولا
direct SQL `DELETE`، ولا force-push، ولا دمج، ولا تغيير للهجرات السابقة أو ملفات المستخدم.
