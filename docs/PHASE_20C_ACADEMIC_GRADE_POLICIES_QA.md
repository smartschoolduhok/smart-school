# Phase 20C — Annual academic grade policies

آخر تحديث: 2026-09-12.

## الحالة الحالية

**الكود والمحاكاة المحلية: PASS. الدمج وSTAGING: PENDING.**

هذه الدفعة تضيف محركًا سنويًا مرنًا لحساب النجاح والإكمال والرسوب والإعفاء والدخول الوزاري. لا تفترض الدفعة أن قرار سنة معينة سيستمر في السنة التالية، ولا تثبّت أسماء صفوف أو عدد مواد أو رصيد قرار داخل الكود. المدرسة تنشئ سياسة مستقلة لكل سنة وصف، تسجل مرجع القرار، تعاين أثرها، ثم تعتمدها صراحةً.

لم يتم الوصول إلى Production، ولم يُنفذ seed/reset بعيد، ولم تُطبّق migration `0033` على STAGING ضمن دليل هذا التقرير حتى الآن.

## قواعد المجال

| نوع الصف | مصدر النتيجة | الإعفاء | النتيجة الأكاديمية | الدخول الوزاري |
|---|---|---|---|---|
| منتهٍ / وزاري | السعي السنوي | ممنوع بقيد في المحرك وقاعدة البيانات | ناجح، مكمل، راسب أو غير مكتمل | غير مطبق، شامل، ناجح فقط، أو ناجح/مكمل ضمن حد سنوي |
| غير منتهٍ | الدرجة النهائية الفعالة، أو السعي عند تحقق الإعفاء | فردي وعام اختياريان | النجاح يتطلب اجتياز كل المواد؛ الإكمال محدود بعدد مواد قابل للضبط | غير مطبق بقيد في المحرك وقاعدة البيانات |

كل سياسة تضبط بصورة مستقلة:

- درجة النجاح.
- رصيد درجات القرار: صفر، خمس، عشر أو قيمة أخرى.
- توزيع آلي يعظّم عدد المواد المنقذة، أو توزيع يدوي موثق.
- قيد «القرار يغير النتيجة فقط» لمنع منح نقاط جزئية بلا أثر.
- أقصى عدد لمواد الإكمال.
- حدود الإعفاء الفردي والعام للصفوف غير المنتهية.
- قاعدة الدخول الوزاري وحد مواد الإكمال للصفوف المنتهية.
- الحد الأدنى للامتحانات الشهرية وطريقة معالجة الكسور.
- مرجع القرار وملاحظاته.

المثال المتفق عليه مغطى باختبار ثابت: الدرجات `44، 35، 29، 46` مع رصيد قرار `10` توزع آليًا `6` على الأولى و`4` على الرابعة؛ تصبح مادتان ناجحتين وتبقى مادتان مكملتين، دون تعديل الدرجات الخام.

## دورة السياسة والتدقيق

1. **مسودة:** قابلة للتعديل والمعاينة ولا تؤثر في أي نتيجة رسمية.
2. **معتمدة:** تصبح أحدث نسخة معتمدة هي مصدر الحساب الرسمي.
3. **مقفلة:** لا تقبل التعديل المباشر.
4. **تعديل جديد:** ينشئ إصدارًا جديدًا كمسودة ويحفظ الإصدار السابق. تبقى آخر نسخة معتمدة فعالة إلى أن يعتمد الإصدار الجديد.

كل إنشاء أو تحديث أو اعتماد أو قفل أو تعديل يسجل في سجل غير قابل للتغيير. الكتابات تستخدم revision/expected version داخل `D1 batch` لمنع الكتابة القديمة والمتزامنة.

التوزيع اليدوي لدرجات القرار لا يعيد كتابة الدرجة الأصلية. يحفظ كإصدار append-only مرتبط بالطالب والسياسة، مع سبب إلزامي وهوية المستخدم وتوقيت الإنشاء. إلغاء النسخة الحالية يعني إحالتها للتاريخ ثم إنشاء نسخة جديدة؛ الحذف النهائي والتعديل التاريخي ممنوعان بواسطة triggers.

## Migration 0033

`migrations/0033_academic_grade_policies.sql` إضافة صرفة، وتضيف:

- `academic_grade_policies` للسياسات السنوية versioned.
- `academic_grade_policy_logs` لسجل القرارات غير القابل للتعديل.
- `academic_grade_decision_sets` لتوزيعات القرار اليدوية versioned.
- جدولي assertions للكتابات المتزامنة.
- indexes للنسخة الحالية والبحث.
- triggers لعزل المدرسة، وصحة نوع السياسة، والتحولات، وحفظ التاريخ.
- `academic_grade_policy_readiness` لقياس تغطية الصفوف، والمسودات، والتعديلات المعلقة.

اختبار الترقية populated يطبق جميع migrations السابقة، يلتقط كل جداول التطبيق القديمة وقيمها، يطبق `0033`، ثم يثبت تطابق جميع القيم السابقة و`foreign_key_check` النظيف. كما يثبت أن وجود مسودة تعديل لا يلغي جاهزية النسخة الرسمية السابقة.

## التكامل الوظيفي

- شاشة «السياسات السنوية» ضمن الدرجات متاحة للإدارة، بقوالب بداية واضحة للصف المنتهي وغير المنتهي.
- الاعتماد معطل حتى حفظ المسودة وتسجيل مرجع القرار.
- توجد معاينة لطالب محدد قبل الاعتماد.
- شاشة الطالب تعرض النتيجة الرسمية، الإعفاء، عدد المواد، الدخول الوزاري ودرجات القرار.
- التوزيع اليدوي محصور بالإدارة ويتطلب سببًا.
- كارت النتيجة الإصدار 5 يجمد policy snapshot وقرار النقاط ونتيجة الدخول الوزاري؛ البطاقات القديمة تبقى كما صدرت.
- الاستيراد من Excel، الحفظ الفردي والجماعي، إعادة الحساب وكروت النتائج تستخدم السياسة المعتمدة للصف.
- التحليلات تضيف تبويبًا إداريًا مبنيًا على الطالب، مع عدادات ناجح/مكمل/راسب/غير مكتمل والدخول الوزاري، بدل عدّ سجلات المواد كطلاب.
- صلاحيات ولي الأمر والمدرس تبقى مقيدة بالطالب/التكليف عبر الحراس القائمة؛ الإدارة فقط تستطيع تغيير السياسات أو التوزيع اليدوي.

## نتائج التحقق المحلي

| البوابة | النتيجة |
|---|---|
| اختبارات Phase 20C المستهدفة | **18/18 PASS** |
| حسابات الدرجات المرنة | **39/39 PASS** |
| استيراد Excel | **82/82 PASS** |
| كروت النتائج | **68/68 PASS** |
| المصفوفة الكاملة | **1469/1469 PASS، 0 fail، 0 skip** |
| TypeScript | **PASS** |
| Frontend build | **PASS** |
| Worker build | **PASS** |
| `npm audit --audit-level=low` | **0 vulnerabilities** |
| `git diff --check` | **PASS** |
| Genuine Local D1 / backup restore | **34 migrations، 59 tables، FK clean** |
| Genuine Local D1 finance regression | **39/39 checks، expected blockers rollback cleanly** |
| الصف الكبير في `import_jobs` | **360,009 bytes restored exactly** |
| لقطة التطبيق والماليات بعد الاستعادة | **exact match / healthy** |

تتضمن الاختبارات: عزل المدارس والأدوار، revision races، منع اعتماد بلا مرجع، حفظ المسودة خارج النتائج الرسمية، استمرارية النسخة الرسمية أثناء تعديل مسودة، انتقال النتيجة بعد اعتماد الإصدار الجديد، منع إعفاء الصف المنتهي، الإعفاء الفردي والعام، الدخول الشامل، حدود الإكمال، توزيع القرار الآلي واليدوي، snapshot كارت النتيجة، الترقية من بيانات populated وعدم تغيير الدرجات الخام.

تحذير البناء المعروف حول chunk `xlsx` الأكبر من 500 kB ما زال تحذير أداء غير مانع ولم ينشأ من منطق السياسات.

## بوابة STAGING المطلوبة قبل الدمج

1. تأكيد أن الفرع والـcommit المقصودين هما نفس مصدر الـPreview.
2. تصدير نسخة كاملة من `smart-school-staging-db` خارج المستودع وحساب SHA-256.
3. فحص history وFK وجميع readiness views، والتأكد أن `0033` وحدها معلقة.
4. استعادة النسخة محليًا، بما فيها أي صف منفرد كبير عبر parameter binding، وإثبات التطابق قبل الترقية.
5. تطبيق `0033` على النسخة المستعادة وإثبات بقاء كل قيم الجداول القديمة كما هي.
6. عند سلامة كل ما سبق فقط، تطبيق `0033` على STAGING وحدها.
7. إعادة فحص migration history وFK/readiness وعدم وجود migrations معلقة.
8. QA مصادق عليه: إنشاء سياسة منتهية وغير منتهية لسجلات QA فقط، معاينة، اعتماد، قفل، تعديل مسودة، توزيع قرار، كارت نتيجة وتحليلات.
9. أرشفة/تعطيل سجلات QA المؤقتة دون `DELETE`، وتوثيق النتائج والبصمات.

أي فشل في النسخ أو الاستعادة أو preflight أو التكافؤ يوقف التطبيق البعيد. لا seed/reset، لا وصول إلى Production، ولا تعديل لملفات migrations المطبقة.

## قرار القبول المحلي

Phase 20C جاهزة للمراجعة ولبوابة STAGING، وليست بعد مقبولة للدمج النهائي. لا تصبح السياسة فعالة في مدرسة حقيقية بمجرد نشر الواجهة؛ يجب تطبيق `0033` ثم إنشاء واعتماد سياسة موثقة لكل صف وسنة. عند غياب سياسة معتمدة، يستمر السلوك الأكاديمي القديم ولا يخترع النظام قرارًا وزاريًا.
