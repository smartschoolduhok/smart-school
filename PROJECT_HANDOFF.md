# Smart School — Project Handoff

آخر تحديث: 2026-09-13.

## 1. ملخص الحالة

النظام تطبيق عربي RTL متعدد المدارس مبني على React/Hono/Cloudflare D1. الوحدات الفعلية تشمل الإدارة، الطلبة والتسجيلات، المواد والدرجات، النتائج، الجدول، Excel، الكتب الرسمية والطباعة، الأقساط والإيصالات، الخزنة، الموظفين والرواتب، والإعدادات.

Phase 20A1 ودفعة ما بعد التدقيق وPhase 20B وPhase 20C مندمجة في `main`. migrations `0029`–`0033` مطبقة على STAGING دون Production، ولا توجد migrations معلّقة هناك. Phase 20B أغلقت حساب الطالب المالي وخطط التقسيط الاختيارية وإيصال A4 وعرض ولي الأمر للقراءة فقط بعد backup وpreflight واستعادة محلية وQA وظيفي ناجح.

Phase 20C أضافت سياسة سنوية versioned لكل صف لحساب النجاح والإكمال والرسوب والإعفاء والدخول الوزاري ودرجات القرار دون تثبيت قرار سنة بعينها في الكود. نجحت المحاكاة المحلية وSTAGING QA، ودُمج PR `#42` في commit `5856e615bcf48af4d43a1bd86043a2e4533cf4e7`، ثم نجحت Quality Gates ونشر Cloudflare Pages للـcommit نفسه. بعد تنظيف QA بقيت readiness الأكاديمية `not_configured` بصورة متوقعة؛ لا تُعتمد نتائج رسمية قبل إدخال سياسة حقيقية موثقة لكل صف وسنة.

Phase 20D.1 مكتملة ومندمجة عبر PR `#43` في commit `87419ea53d36344033221046cd1c76f6f1361a9b`. تضيف دورة `draft → published → withdrawn` لكروت النتائج، وتجعل المنشور فقط ظاهرًا لولي الأمر وقابلًا للتحقق والطباعة. نجحت Quality Gates وCloudflare والنسخة الكاملة وpreflight والاستعادة المحلية وSTAGING QA المصادق عليه. طُبّقت migration `0034` وحدها على STAGING؛ لم يُمس Production.

Phase 20D.2 مكتملة ومندمجة في `main` عبر [PR #44](https://github.com/smartschoolduhok/smart-school/pull/44) وcommit `333df1b8845772a643bd35e4f5611f0cab79bfbf`. تجعل النتيجة الرسمية المنشورة مصدر قرار الانتقال السنوي: ناجح غير منتهٍ يترفع، ناجح منتهٍ يتخرج، راسب يعيد، ومكمل/غير مكتمل يبقى مفتوحًا حتى نتيجة لاحقة. التنفيذ يثبت `result_card_id` و`publication_revision` في سجل immutable وينفذ القرار والتسجيل المستهدف ذرّيًا. طُبّقت `0035` وحدها على STAGING بعد backup وتطابق الاستعادة المحلية؛ السجل `36/36` بلا pending، مع FK وreadiness سليمتين.

دليل Phase 20D.2 الكامل في `docs/PHASE_20D2_OFFICIAL_PROMOTION_QA.md`: backup بحجم `1,237,464` bytes وSHA-256 `810E0E19FE5AFCB56E21A2880AED8165E096826EA59E7B2C18558399069FB3B8`، وعدم تغير أي صف سابق في الجداول التطبيقية الـ`59` حتى بعد QA. marker `PH20D2-b6028291af` مؤرشف/معطل، والسنة المستقبلية `id=6` غير فعالة؛ القرارات الثلاثة ومصادرها وأثر الهدف محفوظة كما يتطلب عقد immutability. نجح فحص RTL بعرض الهاتف والمعاينة والتأكيد والإلغاء بالكيبورد في النمطين.

Phase 20D.3 منفذة محليًا على فرع `codex/phase-20d3-result-cards-analytics` وتنتظر PR/Preview. تضيف Result Card schema v6 بقالب قرار مختصر للصف المنتهي وقالب أكاديمي للصف غير المنتهي، وعناوين عربية/إنكليزية، وأسماء مواد الإكمال/الرسوب/الإعفاء وسبب الدخول الوزاري. تحليل الإدارة يفصل snapshots المنشورة الثابتة عن الحساب الحي ويعرض النجاح والإكمال والرسوب والإعفاء والدخول الوزاري والقرار السنوي بفلاتر مستقلة. لا توجد migration جديدة؛ نجحت `1500/1500` regression وD1 المحلي والاستعادة والبناء. الدليل في `docs/PHASE_20D3_RESULT_CARD_ANALYTICS_QA.md`.

## 2. البنية

| الجزء | المكان | المسؤولية |
|---|---|---|
| Router وguards | `src/App.tsx` | المسارات، lazy loading، وحراسة الأدوار |
| الواجهة المشتركة | `src/components/` | القائمة، الرأس، اختيار المدرسة والطباعة |
| صفحات المجال | `src/modules/` | واجهات الوحدات حسب المجال |
| API client | `src/lib/api.ts` | طلبات الواجهة وعقود الاستجابة |
| منطق المجال | `src/lib/*.ts` | الحسابات، التخطيط، RBAC، والتحقق |
| Worker/API | `src/worker.ts` | Hono routes، tenant targeting، معاملات D1 |
| قاعدة البيانات | `migrations/` | schema، indexes، triggers، readiness |
| الاختبارات | `test/` و`scripts/` | unit/integration/D1/restore/regression |

تدفق الطلب: `React → api.ts → Hono route → domain/DB helpers → D1`.

## 3. التنقل الحالي

القائمة لا تعرض كل الصفحات كأزرار متساوية. توجد لوحة التحكم وست مجموعات قابلة للفتح:

1. شؤون الطلاب.
2. التعليم والجدول.
3. المالية والموظفون.
4. التقارير والوثائق.
5. البيانات.
6. الإدارة والإعدادات.

العناصر تُفلتر حسب الدور، وتتحول «الطلاب» إلى «أبنائي» لولي الأمر. الميزات المستقبلية غير المنفذة لا تظهر كروابط ميتة.

## 4. المصادقة والوصول

الأدوار الأساسية: `system_admin`, `school_owner`, `principal`, `vice_principal`, `teacher`, `accountant`, `registrar`, `parent`.

- JWT مدته ثماني ساعات، يحمل `jti` و`auth_version`، والخروج يلغي الجلسة.
- كلمات المرور الجديدة PBKDF2-HMAC-SHA256 مع salt عشوائي؛ legacy SHA-256 يرقّى بعد دخول ناجح.
- المستخدم المدرسي مقيد بـ`school_id` من الجلسة؛ مدير النظام يحدد المدرسة المستهدفة صراحةً.
- `parent_student_links` هو مصدر صلاحية ولي الأمر للطالب، دون تخمين من اسم/هاتف.
- `teacher_employee_links` يربط مستخدم المدرس بسجل الموظف، ثم تحدد الأحمال التدريسية موارده الأكاديمية.
- لا يكفي إخفاء رابط في UI؛ كل route حرجة تعيد التحقق في الـAPI.

## 5. ثوابت الماليات والدرجات

- العمليات الجديدة بالـIQD وبقيم صحيحة ضمن safe integer.
- الأقساط والدفعات والإيصالات تستخدم idempotency وقيود D1 لمنع التكرار والزيادة.
- إلغاء الإيصال لا يعكس المال؛ إلغاء الدفعة هو الذي ينشئ الأثر العكسي.
- خطة التقسيط metadata اختيارية فوق القسط، ولا تنشئ حركة مالية. نسخ الخطة محفوظة وغير قابلة للحذف أو التعديل التاريخي.
- إيصالات الإصدار الجديد تحفظ snapshot للحساب والخطة والمستلم، ويمكن ربط إيصال بديل بمستند ملغى دون عكس المال.
- ولي الأمر يرى حساب وإيصالات الابن المرتبط عبر endpoints للقراءة فقط؛ تعطيل الرابط يسحب الوصول فورًا.
- دفع/إلغاء الراتب متزامن ذريًا مع قيد الخزنة.
- إلغاء حركة الخزنة exactly-once حتى مع طلبين متزامنين.
- `business_date` محسوب بتوقيت `Asia/Baghdad`.
- اليوم المغلق لا يقبل حركات لاحقة، والرصيد الافتتاحي مشتق من دفتر القيود السابق.
- تعديل الدرجة وتسجيل audit عملية واحدة؛ `revision` يمنع overwrite من نسخة قديمة.
- المسودة السنوية للدرجات لا تؤثر في النتائج. تبقى آخر نسخة معتمدة فعالة أثناء إعداد تعديل جديد، ولا ينتقل الحساب إلا بعد اعتماد صريح موثق.
- درجات القرار لا تعيد كتابة الدرجات الخام؛ التوزيع اليدوي يحفظ كسجل versioned مع سبب، والصفوف المنتهية لا تقبل الإعفاء.
- كارت النتيجة الجديد يبدأ مسودة. النشر يتطلب كارتًا كاملًا وسياسة معتمدة موثقة، والسحب يتطلب سببًا وسجلًا غير قابل للتغيير.
- ولي الأمر يرى فقط النتائج المنشورة للابن المرتبط؛ سحب النتيجة أو تعطيل الرابط يسحب الوصول فورًا.
- الانتقال السنوي الرسمي لا يقبل اختيار النتيجة يدويًا؛ يُشتق من كارت منشور كامل وسياسة موثقة، وتُثبت نسخة النشر المستخدمة في سجل غير قابل للتغيير.
- لا يمكن سحب كارت نتيجة بعد استخدامه في انتقال سنوي. حالات الإكمال أو النتيجة غير المكتملة لا تُقفل تسجيل الطالب.

## 6. الترحيلات

يوجد على `main` وعلى STAGING عدد `36` migration حتى `0035`؛ طُبّقت `0035` مرة واحدة وأخيرة دون pending. Phase 20D.3 لا تضيف migration. المصدر المعتمد للترتيب هو أسماء الملفات داخل `migrations/`، بما فيها ملفا `0014` التاريخيان. لم تُطبّق هذه الهجرة على Production.

| المجموعة | النطاق |
|---|---|
| `0001`–`0019` | الأساس، الأكاديميات، الأمن، الإعدادات والنتائج |
| `0020`–`0022` | التسجيلات، ديانة الطالب، ومساقات الديانة |
| `0023`–`0027` | تأسيس الجدول والقيود والنسخ والاعتماد |
| `0028` | سلامة الأقساط والدفعات والإيصالات |
| `0029` | روابط الوصول للموارد |
| `0030` | revisions وتدقيق الدرجات |
| `0031` | سلامة الخزنة والرواتب والإقفال |
| `0032` | خطط تقسيط versioned وsnapshots إيصالات الإصدار 2 |
| `0033` | سياسات الدرجات السنوية وسجلها وتوزيعات القرار اليدوية والجاهزية |
| `0034` | دورة نشر كروت النتائج وسجل السحب وجاهزية النشر |
| `0035` | ربط قرار الانتقال السنوي بنسخة النتيجة الرسمية وسجل القرار وجاهزيته |

لا تعدّل migration مطبقًا؛ أضف ملفًا جديدًا واختبر fresh DB وترقية قاعدة populated.

## 7. التشغيل والجودة

```bash
npm ci
npm run db:migrate
npm run db:seed
npm run dev
```

بوابات الـPR:

```bash
npm audit --audit-level=low
npm run typecheck
npm run test:regressions
npm run test:finance-seed:local
npm run test:backup-restore:local
npm run test:official-promotion:local
npm run build
```

`test:backup-restore:local` ينشئ مصدرًا وقاعدة استعادة مؤقتين، يصدّر D1 محليًا، يستوردها، يقارن جميع جداول التطبيق، ثم يعيد فحص ثوابت الماليات.

## 8. البيئات

- `wrangler.jsonc`: STAGING فقط.
- scripts المسماة `:local` وأوامر `db:migrate/db:seed`: Local فقط.
- أي `--remote`: تغيير تشغيلي يحتاج target صريحًا ونسخة احتياطية واعتمادًا.
- Production: config وأسرار وقرار إصدار منفصلون.
- `seed.sql`: disposable Local/Demo فقط.

راجع `ENVIRONMENT.md` و`DEPLOYMENT.md` قبل أي عمل بعيد.

## 9. دين تقني باقٍ

1. `src/worker.ts` كبير جدًا (أكثر من 12 ألف سطر) ويحتاج تفكيكًا تدريجيًا حسب المجال مع الحفاظ على العقود والاختبارات.
2. بعض صفحات المجال ما زالت كبيرة؛ الأفضل استخراج hooks/components عند تعديلها مستقبلًا، لا refactor شاملًا عالي المخاطر.
3. يلزم Pilot فعلي مع موظف مدرسة آخر يستخدم المسارات اليومية دون مساعدة مباشرة، مع تسجيل نقاط التعثر بدل افتراضها.
4. نجح تمرين استعادة نسخة STAGING الحقيقية محليًا؛ تبقى الحاجة إلى سياسة دورية موثقة للنسخ والاستعادة ضمن بيئة غير Production.
5. لا توجد حاليًا إشعارات خارجية أو تطبيق هاتف أو وضع عمل offline؛ تبقى ميزات مستقبلية لا blockers للاستقرار.

## 10. أولويات المستقبل بعد قبول دفعة التثبيت

1. Smoke test موثّق على نشر `main` في STAGING، ثم Pilot فعلي مع مستخدم آخر دون توجيه مباشر.
2. مراقبة تشغيلية للأخطاء والأداء وسجل عمليات إداري قابل للبحث.
3. استخراج route modules من `worker.ts` تدريجيًا دون تغيير السلوك.
4. مركز مهام وتنبيهات حسب الدور بدل إضافة أزرار جديدة للقائمة، استنادًا إلى نتائج الـPilot.
5. إشعارات الأقساط/النتائج بعد قياس استخدام بوابة ولي الأمر وموافقات الخصوصية.
6. تقارير قابلة للتخصيص وتصدير مجدول، مع نسخ احتياطي تشغيلي واختبارات استعادة دورية.

Phase 20B وPhase 20C وPhase 20D.1 وPhase 20D.2 مقبولة ومندمجة. migrations حتى `0035` مطبقة على STAGING. Phase 20D.3 اجتازت محليًا `1500/1500` regression وفحوص D1 والاستعادة والبناء وتنتظر PR/Preview واختبار العرض المنشور؛ لا دمج تلقائي ولا وصول إلى Production.
